<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 flex justify-center p-10">
        <a href="/" class="absolute top-10 left-20 text-blue-600">Back to home</a>
        <div class="container w-[85%]">
            <h2 class="text-center w-full text-white text-3xl font-bold uppercase">All Users</h2>
            <div class="w-full h-[90%] mt-5 p-5 flex flex-wrap gap-2">
                <% allUser.forEach(user =>{ %>
                    <div class="user w-60 h-80 bg-zinc-800 rounded-lg p-2">
                        <div class="w-full h-48 rounded-lg overflow-hidden">
                            <img class="w-full h-full object-cover" src="<%= user.image %>" alt="">
                        </div>
                        <h2 class="text-white font-semibold text-xl mt-3 "><%= user.name%></h2>
                        <h3 class="text-white/40 text-sm mt-1 "><%= user.email  %></h3>
                        <div class="w-full mt-4  flex justify-between items-center ">
                            <a href="/edit/<%= user._id  %>" class="text-xs text-white bg-yellow-700 rounded-md py-1 px-4">Edit</a>
                            <a href="/delete/<%= user._id  %>" class="text-xs text-white bg-red-700 rounded-md py-1 px-4">Delete</a>
                        </div>
                    </div>
                <% }) %>
            </div>
        </div>
    </div>
</body>
</html>